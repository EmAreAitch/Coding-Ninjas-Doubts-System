<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Coding Ninjas Doubt System" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :all, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-background text-text font-sans">
    <!-- Top Bar -->
    <header class="bg-surface border-b border-border">
      <div class="px-3 sm:px-6 py-3">
        <!-- Mobile Layout (< sm) -->
        <div class="flex sm:hidden items-center justify-between">
          <!-- CN Logo -->
          <div class="bg-orange-500 text-white font-bold px-2 py-1 text-sm">
            CN
          </div>
          
          <!-- Mobile Menu Button -->
          <button id="mobile-menu-btn" class="p-2 rounded-md hover:bg-gray-100" type="button">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>
        
        <!-- Desktop Layout (>= sm) -->
        <div class="hidden sm:flex items-center justify-between">
          <!-- Left side: CN + nav links -->
          <div class="flex items-center space-x-4">
            <!-- CN orange box -->
            <div class="bg-orange-500 text-white font-bold px-3 py-1">
              CN
            </div>
            <!-- Nav links -->
            <nav class="flex items-center space-x-2 text-sm lg:text-base">
              <% @nav_links = [
                ["Home", root_path],
                ["Raise Doubt", new_doubt_path],
                ["Solve Doubt", doubt_assignments_path],
                ["Dashboard", dashboard_path]
              ] %>
              <% @nav_links.each_with_index do |(text, path), index| %>
                <%= link_to text, path, class: "hover:text-accent whitespace-nowrap" %>
                <% unless index == @nav_links.size - 1 %>
                  <span class="hidden lg:inline">|</span>
                <% end %>
              <% end %>
            </nav>
          </div>
          
          <!-- Right side: User info + Logout -->
          <div class="flex items-center space-x-2 lg:space-x-3">
            <!-- User Type Indicator -->
            <% if user_signed_in? %>
              <div class="flex items-center space-x-1 lg:space-x-2">
                <!-- User type badge -->
                <span class="<%= current_user.is_a?(Student) ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800' %> text-xs font-medium px-2 py-1 rounded whitespace-nowrap">
                  <span class="hidden md:inline"><%= current_user.is_a?(Student) ? '👨‍🎓 Student' : '👨‍🏫 TA' %></span>
                  <span class="md:hidden"><%= current_user.is_a?(Student) ? '👨‍🎓' : '👨‍🏫' %></span>
                </span>
                <!-- User name/email - hidden on smaller screens -->
                <span class="hidden lg:block text-sm text-gray-600 max-w-24 xl:max-w-none truncate">
                  <%= current_user.name %>
                </span>
              </div>
            <% end %>
            
            <!-- Logout button -->
            <%= button_to "Logout",
                destroy_user_session_path,
                method: :delete,            
                class: "bg-accent text-white px-2 lg:px-3 py-1 text-sm font-medium shadow hover:bg-accent/90 whitespace-nowrap" %>
          </div>
        </div>
        
        <!-- Mobile Menu Dropdown -->
        <div id="mobile-menu" class="hidden sm:hidden mt-3 pb-2 border-t border-border pt-3">
          <nav class="flex flex-col space-y-3">            
            <% @nav_links.each do |text, path| %>
              <%= link_to text, path, class: "block py-2 px-3 rounded-md hover:bg-gray-100 text-sm" %>
            <% end %>
            
            <!-- Mobile User Info -->
            <% if user_signed_in? %>
              <div class="mt-3 pt-3 border-t border-border">
                <!-- Stack on very small screens, side-by-side on larger mobile -->
                <div class="flex flex-col xs:flex-row xs:items-center xs:justify-between space-y-2 xs:space-y-0">
                  <div class="flex items-center space-x-2 flex-1 min-w-0">
                    <!-- User type badge -->
                    <div class="<%= current_user.is_a?(Student) ? 'bg-blue-100 text-blue-800' : 'bg-green-100 text-green-800' %> text-xs font-medium px-2 py-1 rounded flex-shrink-0">
                      <span class="block xs:hidden"><%= current_user.is_a?(Student) ? '👨‍🎓 Student' : '👨‍🏫 TA' %></span>
                      <span class="hidden xs:block"><%= current_user.is_a?(Student) ? '👨‍🎓 Student' : '👨‍🏫 Teaching Assistant' %></span>
                    </div>
                    <!-- User name -->
                    <div class="text-sm text-gray-600 truncate">
                      <%= current_user.name %>
                    </div>
                  </div>
                  <!-- Logout button - full width on tiny screens, auto on larger -->
                  <%= button_to "Logout",
                      destroy_user_session_path,
                      method: :delete,            
                      class: "bg-accent text-white px-3 py-2 text-sm font-medium shadow hover:bg-accent/90 rounded w-full xs:w-auto flex-shrink-0" %>
                </div>
              </div>
            <% end %>
          </nav>
        </div>
      </div>
    </header>
    
    <%= render "flash" %>
    
    <main class="container mx-auto px-3 sm:px-6 py-4 sm:py-6 max-w-7xl">
      <%= yield %>
    </main>
  </body>
</html>
